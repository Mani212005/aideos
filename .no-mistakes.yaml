# Per-repo no-mistakes configuration for Aideos
commands:
  test: 'npm test && npm run validate'
  lint: 'npx tsc --noEmit && (test -d editor/node_modules || npm --prefix editor ci) && npx tsc --noEmit -p editor/tsconfig.app.json'
